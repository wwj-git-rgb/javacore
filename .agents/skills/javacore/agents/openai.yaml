interface:
  display_name: "Javacore"
  short_description: "Repo-specific patterns and workflows for javacore"
  default_prompt: "Use the javacore repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true